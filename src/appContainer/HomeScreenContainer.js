import MainTabContainer from "./MainTabContainer"

const HomeScreenContainer = (navigation) => {
  return (
    <MainTabContainer navigation={navigation} />
  )
}

export default HomeScreenContainer

